
// PascalCasing -> every letter is capitalize
function Message() {
    const name = "John";
    // JSX (JavaScript XML) -> it use HTML code inside of JavaScript code
    return <h1>Hello {name}!</h1>;
}

export default Message; // export -> to import different file,  default -> one file only, without default -> multiple files